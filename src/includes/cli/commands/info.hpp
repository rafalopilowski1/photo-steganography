#pragma once
namespace cli {
    /**
     * Funckja ukazujące podstawowe informacje o obrazie
     * @param fileInputPath Ścieżka do obrazu
     */
    auto info(const std::string &fileInputPath) -> void;
}